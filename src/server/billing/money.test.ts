import { describe,expect,it } from "vitest";
import { allocateMoney,dashboardMetrics,normalizeMonthly } from "./money";

const yearly={amountMinor:9900n,currency:"EUR",billingModel:"fixed_yearly" as const,billingInterval:"year" as const,status:"active" as const};
describe("money",()=>{it("normalizes annual commitments with integer minor units",()=>expect(normalizeMonthly(yearly)).toEqual({amountMinor:825n,currency:"EUR"}));it("allocates without exceeding 10,000 basis points",()=>{expect(allocateMoney(10000n,[5000,2500])).toEqual([5000n,2500n]);expect(()=>allocateMoney(100n,[6000,5000])).toThrow();});it("does not double count linked subscription cost in forecast",()=>{const metrics=dashboardMetrics({baseCurrency:"EUR",now:new Date("2026-08-12T00:00:00Z"),subscriptions:[{id:"year",...yearly}],costs:[{amountMinor:825n,currency:"EUR",kind:"subscription",subscriptionId:"year",periodStart:new Date("2026-08-01"),periodEnd:new Date("2026-08-31")},{amountMinor:400n,currency:"EUR",kind:"usage",periodStart:new Date("2026-08-01"),periodEnd:new Date("2026-08-31")}],wasteSubscriptionIds:["year"]});expect(metrics).toEqual({monthToDate:1225n,monthlyCommitment:825n,forecast:1225n,annualized:9900n,potentialWaste:825n});});});
